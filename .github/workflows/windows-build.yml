name: Windows build

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - "phase/**"
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Restore
        run: dotnet restore Glass.sln

      - name: Build Windows application
        run: dotnet build src/Glass.App/Glass.App.csproj --configuration Release --no-restore --property:Platform=x64

      - name: Test Core
        run: dotnet test tests/Glass.Core.Tests/Glass.Core.Tests.csproj --configuration Release --no-restore
